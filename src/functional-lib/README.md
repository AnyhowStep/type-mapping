A collection of factory functions that create `Mapper<>` instances.

This is intended to reduce the number of `Mapper<>` functions
that the user has to create themselves, lowering the barrier to entry.